services:
  db:
    image: postgres:16-alpine
    container_name: ${PROJECT_NAME}-db
    environment:
      - DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${PROJECT_NAME}-db:5432/${POSTGRES_DB}
      - PROJECT_NAME=${PROJECT_NAME}
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB}
      - DB_PORT=${DB_PORT}
      - APP_NAME=${APP_NAME}
    volumes:
      - ../../ci/db/init.sql:/docker-entrypoint-initdb.d/init.sql
      - ../../pgdata:/var/lib/postgresql/data
    ports:
      - ${DB_PORT}:5432
    networks:
      - custom-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER}"]
      interval: 10s
      timeout: 5s
      retries: 5

  api:
    build:
      context: ../../
      dockerfile: ci/dockers/prod/Dockerfile.api
    container_name: ${PROJECT_NAME}-api
    environment:
      - DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${PROJECT_NAME}-db:5432/${POSTGRES_DB}
      - PROJECT_NAME=${PROJECT_NAME}
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB}
      - DB_PORT=${DB_PORT}
      - NETWORK_NAME=${NETWORK_NAME}
      - SERVER_PORT=${SERVER_PORT}
      - APP_NAME=${APP_NAME}
      - ALLOWED_ORIGINS=${ALLOWED_ORIGINS}
      - SERVER_NAME=${SERVER_NAME}
      - COOKIE_DOMAIN=${SERVER_NAME}
      - ACCESS_JWT_SECRET=${ACCESS_JWT_SECRET}
      - ACCESS_JWT_EXPIRED_SEC=${ACCESS_JWT_EXPIRED_SEC}
      - REFRESH_JWT_SECRET=${REFRESH_JWT_SECRET}
      - REFRESH_JWT_EXPIRED_SEC=${REFRESH_JWT_EXPIRED_SEC}
      - SIGN_UP_JWT_SECRET=${SIGN_UP_JWT_SECRET}
      - SIGN_UP_JWT_EXPIRED_SEC=${SIGN_UP_JWT_EXPIRED_SEC}
      - WEB_URL=${WEB_URL}
      - PORT=${SERVER_PORT:-3000}
      - REDIS_HOST=${PROJECT_NAME}-redis
      - REDIS_PORT=6379
      - REDIS_PASSWORD=${REDIS_PASSWORD}
    ports:
      - "${SERVER_PORT:-3000}:${SERVER_PORT:-3000}"
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy

    networks:
      - custom-network
  web:
    build:
      context: ../../
      dockerfile: ci/dockers/prod/Dockerfile.web
      args:
        - PROJECT_NAME=${PROJECT_NAME}
        - API_BASE_URL=${API_BASE_URL}
        - NEXT_PUBLIC_API_BASE_URL=${NEXT_PUBLIC_API_BASE_URL}
        - NEXT_PUBLIC_PROJECT_NAME=${NEXT_PUBLIC_PROJECT_NAME}
        - WEB_URL=${WEB_URL}
    container_name: ${PROJECT_NAME}-web
    environment:
      - PROJECT_NAME=${PROJECT_NAME}
      - NETWORK_NAME=${NETWORK_NAME}
      - WEB_PORT=${WEB_PORT}
      - APP_NAME=${APP_NAME}
      - API_BASE_URL=${API_BASE_URL}
      - NEXT_PUBLIC_API_BASE_URL=${NEXT_PUBLIC_API_BASE_URL}
      - NEXT_PUBLIC_PROJECT_NAME=${NEXT_PUBLIC_PROJECT_NAME}
      - WEB_URL=${WEB_URL}
      - PORT=${WEB_PORT:-3001}
    ports:
      - "${WEB_PORT:-3001}:${WEB_PORT:-3001}"
    networks:
      - custom-network

  admin:
    build:
      context: ../../
      dockerfile: ci/dockers/prod/Dockerfile.admin
      args:
        - PROJECT_NAME=${PROJECT_NAME}
        - NEXT_PUBLIC_API_BASE_URL=${NEXT_PUBLIC_API_BASE_URL}
        - NEXT_PUBLIC_PROJECT_NAME=${NEXT_PUBLIC_PROJECT_NAME}
        - API_BASE_URL=${API_BASE_URL}
    container_name: ${PROJECT_NAME}-admin
    environment:
      - PROJECT_NAME=${PROJECT_NAME}
      - NETWORK_NAME=${NETWORK_NAME}
      - ADMIN_PORT=${ADMIN_PORT:-3333}
      - APP_NAME=${APP_NAME}
      - NEXT_PUBLIC_API_BASE_URL=${NEXT_PUBLIC_API_BASE_URL}
      - NEXT_PUBLIC_PROJECT_NAME=${NEXT_PUBLIC_PROJECT_NAME}
      - API_BASE_URL=${API_BASE_URL}
      - PORT=${ADMIN_PORT:-3333}

    ports:
      - "${ADMIN_PORT:-3333}:${ADMIN_PORT:-3333}"
    networks:
      - custom-network

  # 호스트 포트를 열지 않는 내부 리버스 프록시.
  # 외부 트래픽(80/443)과 TLS 는 서버 공용 엣지 프록시(Caddy, ci/edge/)가 담당하며,
  # 엣지가 edge 네트워크를 통해 ${PROJECT_NAME}-nginx:80 으로 전달합니다.
  nginx:
    image: nginx:1.25-alpine
    container_name: ${PROJECT_NAME}-nginx
    environment:
      - PROJECT_NAME=${PROJECT_NAME}
      - SERVER_PORT=${SERVER_PORT:-3000}
      - WEB_PORT=${WEB_PORT:-3001}
      - ADMIN_PORT=${ADMIN_PORT:-3333}
      - SERVER_NAME=${SERVER_NAME}
      - NGINX_ENVSUBST_FILTER=PROJECT_NAME|SERVER_PORT|WEB_PORT|ADMIN_PORT|SERVER_NAME
    volumes:
      - ../nginx/templates:/etc/nginx/templates:ro
    depends_on:
      api:
        condition: service_started
      web:
        condition: service_started
      admin:
        condition: service_started
    networks:
      - custom-network
      - edge

  redis:
    image: redis:7-alpine
    container_name: ${PROJECT_NAME}-redis
    # 운영 Redis 는 외부 포트를 노출하지 않고 내부 네트워크로만 접근합니다.
    command: redis-server --appendonly yes --requirepass ${REDIS_PASSWORD}
    environment:
      - REDIS_PASSWORD=${REDIS_PASSWORD}
    volumes:
      - redisdata:/data
    networks:
      - custom-network
    healthcheck:
      test: ["CMD-SHELL", 'redis-cli -a "$$REDIS_PASSWORD" ping | grep -q PONG']
      interval: 10s
      timeout: 5s
      retries: 5

networks:
  custom-network:
    driver: bridge
    external: true
    name: ${NETWORK_NAME}
  # 엣지 프록시(Caddy)와 공유하는 네트워크. 서버 셋업 시 `docker network create edge` 필요.
  edge:
    external: true
    name: ${EDGE_NETWORK_NAME:-edge}

volumes:
  pgdata:
  redisdata:
