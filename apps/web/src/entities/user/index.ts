// `UserTermsBadges` 는 제거했다 — 그 약관은 매장 약관이 아니라 회원이 pawlog 와 맺은
// 계약이라, 매장 화면에서 남의 동의 이력을 보여줄 근거가 없다 (플랫폼 콘솔에 있다).
export { RoleBadge } from "./ui/RoleBadge";
export { ROLE_LABELS, roleLabel, roleBadgeStyle } from "./lib/roleLabel";
export { useMe } from "./model/useMe";
